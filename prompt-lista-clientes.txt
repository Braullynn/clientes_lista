"Utilizando as informações que você possui, preciso que você incremente o seguinte:

1. **Arquivo `database.js`:**
   - Crie uma função que verifique se a tabela `users` já existe. Se a tabela não existir, ela deve ser criada.
   - Após a criação da tabela, adicione um usuário administrador com o login `adm` e a senha `0123456789`.

2. **Página de Login de Usuário:**
   - Crie uma página de login que será a página inicial do projeto. Esta página deve conter:
     - Uma caixa de login com dois campos de entrada: um para o login e outro para a senha.
     - Abaixo dos campos, adicione dois botões: 'Cadastro' e 'Login'.
   - Ao clicar no botão 'Cadastro', deve ser possível gerar um novo usuário e senha na lista `users` no banco de dados.
   - Implemente a função de login, utilizando JWT, bcrypt para criptografia de senhas, e cache do navegador para armazenar a sessão do usuário.

3. **Redirecionamento:**
   - Após o login bem-sucedido, o usuário deve ser redirecionado para a página de cadastro de clientes.

**Observação:** Como sou iniciante em programação, por favor, explique as modificações com comentários no código. Indique também quais arquivos precisam ser criados ou alterados, e onde cada arquivo deve ser localizado no projeto."
